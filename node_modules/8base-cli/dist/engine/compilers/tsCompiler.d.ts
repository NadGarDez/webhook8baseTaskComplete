import { ICompiler } from '../../interfaces/ICompiler';
import { Context } from '../../common/context';
export declare class TypescriptCompiler implements ICompiler {
    readonly files: string[];
    private context;
    constructor(files: string[], context: Context);
    compile(buildDir: string): Promise<string[]>;
    private config;
}
//# sourceMappingURL=tsCompiler.d.ts.map