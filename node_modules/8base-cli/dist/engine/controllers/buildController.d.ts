/// <reference types="node" />
import { Readable } from 'stream';
import { Context } from '../../common/context';
export declare class BuildController {
    static clearBuild(context: Context): void;
    static package(context: Context): Promise<{
        build: Readable;
        meta: Readable;
    }>;
    static compile(context: Context): Promise<{
        compiledFiles: string[];
    }>;
    /**
     * Private functions
     */
    private static packageSources;
    private static packageMetadata;
    private static prepare;
}
//# sourceMappingURL=buildController.d.ts.map