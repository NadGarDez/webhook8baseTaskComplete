import { TableSchema } from '@8base/utils';
import { IGeneratorsCommonConfig, IScreenTable } from '../types';
interface IGenerateProjectFsData {
    tablesList: TableSchema[];
    screen: IScreenTable;
    rootFile?: string;
}
export declare const generateScreen: ({ tablesList, screen, rootFile }: IGenerateProjectFsData, config: IGeneratorsCommonConfig) => {
    [key: string]: string;
};
export {};
