import { GeneratorsConfig, IGeneratorsData } from '../../types';
export declare const generateEditForm: ({ tablesList, tableId, screenName }: IGeneratorsData, { includeColumns }?: GeneratorsConfig) => string;
