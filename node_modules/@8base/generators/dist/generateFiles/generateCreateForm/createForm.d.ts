import { GeneratorsConfig, IGeneratorsData } from '../../types';
export declare const generateCreateForm: ({ tablesList, tableId, screenName }: IGeneratorsData, { includeColumns }?: GeneratorsConfig) => string;
